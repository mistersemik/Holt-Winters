name: Code Linting

on:
  push:
    branches: [ main ]
    paths:
      - '**.py'  # Только при изменении Python-файлов
  pull_request:
    branches: [ '*' ]
    paths:
      - '**.py'

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: Install linters
        run: pip install black flake8 mypy

      - name: Run Black
        run: black --check . || echo "⚠️ Run 'black .' locally to fix formatting"

      - name: Run Flake8
        run: flake8 .

      - name: Run Mypy
        run: mypy .
